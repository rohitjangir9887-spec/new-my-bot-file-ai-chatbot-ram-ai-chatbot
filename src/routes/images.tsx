import { createFileRoute } from '@tanstack/react-router'; import { ProfessionalPage } from '@/components/professional/ProfessionalPage';
export const Route = createFileRoute('/images')({ component: Images });
function Images(){return <ProfessionalPage title="Image Studio" subtitle="Create, organize and review AI-generated visuals."><div className="grid md:grid-cols-3 gap-4">{['Create Image','Image Library','Generation History'].map((x,i)=><div key={x} className="min-h-44 rounded-2xl border border-white/10 bg-white/[.035] p-5"><h2 className="font-semibold">{x}</h2><p className="mt-2 text-sm text-white/40">{i===0?'Start a new image workflow.':'Your workspace will appear here.'}</p>{i===0&&<button className="mt-5 rounded-xl bg-primary px-4 py-2 text-sm font-semibold">New Generation</button>}</div>)}</div></ProfessionalPage>}
